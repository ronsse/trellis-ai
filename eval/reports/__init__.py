"""Eval reports — operator-facing renderers and artifacts.

PNG charts and other generated artifacts land here at runtime; the
directory is gitignored except for ``.gitkeep``. The renderers
themselves (e.g. :mod:`eval.reports.program_convergence_chart`) are
tracked source.
"""
