# Silent-fallback audit — 2026-05

Generated by `scripts/audit_silent_fallbacks.py`. Re-run to refresh; the script is deterministic so diffs are meaningful.

- **Source root scanned:** `src`
- **Total candidate `except` sites flagged:** **153**

## Bucket totals

- **DEFECT**: 112 (73.2%)
- **GRACEFUL-DEGRADATION**: 9 (5.9%)
- **GUARD**: 32 (20.9%)
- **TEST-ONLY**: 0 (0.0%)

> The script flags every `except` clause that doesn't `raise` somewhere in its body. Bucket assignment is heuristic (AST shape + catch breadth + function name) and must be confirmed by a human before any code change.

## Per-directory breakdown

| Subpackage | Total | DEFECT | GRACEFUL | GUARD | TEST-ONLY |
|---|---:|---:|---:|---:|---:|
| `trellis/` | 118 | 88 | 8 | 22 | 0 |
| `trellis_cli/` | 15 | 9 | 0 | 6 | 0 |
| `trellis_api/` | 9 | 7 | 1 | 1 | 0 |
| `trellis_sdk/` | 4 | 3 | 0 | 1 | 0 |
| `trellis_workers/` | 7 | 5 | 0 | 2 | 0 |

## DEFECT — known critical

- **§4.1 LLMExtractor parse swallow** — `trellis/extract/llm.py:251` in `_try_json_loads` — `except json.JSONDecodeError:` (pattern=`return-empty`, catch=`specific`)
- **§4.2 worker miner parse swallow** — `trellis_workers/learning/miner.py:211` in `_parse_candidates` — `except (json.JSONDecodeError, ValueError):` (pattern=`log-return-empty`, catch=`specific`)
- **§4.7 MutationExecutor broad-catch / event-log swallow** — `trellis/mutate/executor.py:190` in `execute` — `except Exception as exc:` (pattern=`other`, catch=`Exception`)

## Per-file findings

### `trellis/`

#### `trellis/classify/classifiers/llm.py` (1 sites — GUARD=1)

- **line 56** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`classify`

  ```python
  except RuntimeError:
              loop = None
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/classify/refresh.py` (3 sites — DEFECT=3)

- **line 287** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_default_context_builder`

  ```python
  except Exception:
              logger.debug(
                  "existing_tags_malformed",
                  item_id=doc.get("doc_id"),
              )
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 316** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_parse_classified_at`

  ```python
  except (TypeError, ValueError):
          return None
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

- **line 341** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_emit_tags_refreshed`

  ```python
  except Exception:
          logger.exception(
              "tags_refreshed_emit_failed",
              item_id=item_id,
          )
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/core/base.py` (1 sites — GUARD=1)

- **line 25** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`get_version`

  ```python
  except ImportError:
          return "0.0.0-dev"
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/extract/dispatcher.py` (3 sites — DEFECT=3)

- **line 277** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_emit_fallback`

  ```python
  except Exception:
              logger.exception(
                  "extractor_fallback_emit_failed",
                  source_hint=source_hint,
                  reason=reason,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 295** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_collect_findings`

  ```python
  except Exception:
                  logger.exception(
                      "validator_raised",
                      validator=getattr(validator, "name", validator.__class__.__name__),
                      source_hint=source_hint,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 385** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_emit_extraction_rejected`

  ```python
  except Exception:
              logger.exception(
                  "extraction_rejected_emit_failed",
                  source_hint=source_hint,
                  extractor_used=extractor_used,
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/extract/llm.py` (2 sites — GRACEFUL-DEGRADATION=1, GUARD=1)

- **line 251** — bucket=**GRACEFUL-DEGRADATION**, pattern=`return-empty`, catch=`specific`, fn=`_try_json_loads`

  ```python
  except json.JSONDecodeError:
          return None
  ```

  _Reviewer note:_ function named try_/maybe_ — empty return is the conventional signal.

- **line 362** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_clamp_confidence`

  ```python
  except (TypeError, ValueError):
          return default
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/extract/registry.py` (1 sites — DEFECT=1)

- **line 100** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`load_entry_points`

  ```python
  except Exception:
                  logger.exception("extractor_plugin_init_failed", name=spec.name)
                  continue
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/feedback/recording.py` (4 sites — DEFECT=4)

- **line 178** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`record_feedback`

  ```python
  except Exception as exc:
              event_log_error = exc
              logger.exception(
                  "feedback_event_emit_failed",
                  run_id=feedback.run_id,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 198** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`record_feedback`

  ```python
  except Exception as exc:
              outcome_error = exc
              logger.exception(
                  "feedback_outcome_emit_failed",
                  run_id=feedback.run_id,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 278** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`reconcile_feedback_log_to_event_log`

  ```python
  except Exception:
              result.failed += 1
              result.missing_feedback_ids.append(fb.feedback_id)
              logger.exception(
                  "feedback_reconcile_emit_failed",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 304** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_parse_timestamp`

  ```python
  except (TypeError, ValueError):
          return None
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

#### `trellis/learning/tuners/promotion.py` (1 sites — GUARD=1)

- **line 128** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_compute_effect_size`

  ```python
  except (TypeError, ValueError):
              continue
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/learning/tuners/rule_tuner.py` (1 sites — GRACEFUL-DEGRADATION=1)

- **line 447** — bucket=**GRACEFUL-DEGRADATION**, pattern=`log-only`, catch=`specific`, fn=`run`

  ```python
  except ValueError:
                      logger.warning(
                          "rule_tuner.cursor_parse_failed",
                          tuner=self._tuner_name,
                          cursor=raw_cursor,
  ```

  _Reviewer note:_ logs and continues — likely best-effort; verify intent.

#### `trellis/mcp/server.py` (34 sites — DEFECT=31, GRACEFUL-DEGRADATION=3)

- **line 134** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_get_memory_extractor`

  ```python
  except Exception:
          logger.debug("memory_extractor_init_failed", exc_info=True)
          _memory_extractor = None
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 152** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_build_llm_client`

  ```python
  except Exception:
          logger.debug("llm_client_registry_failed", exc_info=True)
          client = None
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 182** — bucket=**GRACEFUL-DEGRADATION**, pattern=`log-only`, catch=`specific`, fn=`_build_llm_client_from_env`

  ```python
  except ModuleNotFoundError:
              logger.debug("llm_client_openai_not_installed")
  ```

  _Reviewer note:_ logs and continues — likely best-effort; verify intent.

- **line 184** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_build_llm_client_from_env`

  ```python
  except Exception:
              logger.debug("llm_client_openai_init_failed", exc_info=True)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 194** — bucket=**GRACEFUL-DEGRADATION**, pattern=`log-only`, catch=`specific`, fn=`_build_llm_client_from_env`

  ```python
  except ModuleNotFoundError:
              logger.debug("llm_client_anthropic_not_installed")
  ```

  _Reviewer note:_ logs and continues — likely best-effort; verify intent.

- **line 196** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_build_llm_client_from_env`

  ```python
  except Exception:
              logger.debug("llm_client_anthropic_init_failed", exc_info=True)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 217** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`resolve`

  ```python
  except Exception:
              logger.debug("alias_resolver_query_failed", alias=alias)
              return matches
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 266** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_run_memory_extraction`

  ```python
  except Exception:
          logger.debug("memory_extraction_failed", doc_id=doc_id, exc_info=True)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 288** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`Exception`, fn=`_get_minhash_index`

  ```python
  except Exception:
          logger.debug("minhash_index_init_failed")
          return None
  ```

  _Reviewer note:_ broad catch (Exception); logs warning then returns empty — classic silent-fallback shape

- **line 342** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_context`

  ```python
  except Exception:
          logger.exception("get_context_doc_search_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 363** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_context`

  ```python
  except Exception:
          logger.exception("get_context_graph_search_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 378** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_context`

  ```python
  except Exception:
          logger.exception("get_context_trace_search_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 404** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_context`

  ```python
  except Exception:
              logger.debug("get_context_session_dedup_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 439** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_context`

  ```python
  except Exception:
              logger.debug("get_context_pack_emit_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 450** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_context`

  ```python
  except Exception:
          logger.debug("token_tracking_failed", operation="get_context")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 472** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`save_experience`

  ```python
  except Exception as exc:
          return f"Error: Invalid trace JSON — {exc}"
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 598** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`save_memory`

  ```python
  except Exception:
          logger.debug("save_memory_minhash_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 610** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`save_memory`

  ```python
  except Exception:
          logger.debug("save_memory_minhash_index_add_failed", doc_id=stored_id)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 627** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`save_memory`

  ```python
  except Exception:
          logger.debug("memory_stored_event_emission_failed", doc_id=stored_id)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 672** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_lessons`

  ```python
  except Exception:
          logger.debug("token_tracking_failed", operation="get_lessons")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 715** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_graph`

  ```python
  except Exception:
          logger.debug("token_tracking_failed", operation="get_graph")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 876** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`search`

  ```python
  except Exception:
          logger.debug("token_tracking_failed", operation="search")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 983** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_objective_context`

  ```python
  except Exception:
              logger.debug("token_tracking_failed", operation="get_objective_context")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 985** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`get_objective_context`

  ```python
  except Exception:
          logger.exception("get_objective_context_failed")
          return f"Error: failed to assemble objective context for: {intent}"
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1096** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_task_context`

  ```python
  except Exception:
              logger.debug("token_tracking_failed", operation="get_task_context")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1098** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`get_task_context`

  ```python
  except Exception:
          logger.exception("get_task_context_failed")
          return f"Error: failed to assemble task context for: {intent}"
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1213** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`get_sectioned_context`

  ```python
  except Exception:
              logger.debug("token_tracking_failed", operation="get_sectioned_context")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1215** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`get_sectioned_context`

  ```python
  except Exception:
          logger.exception("get_sectioned_context_failed")
          return f"Error: failed to assemble sectioned context for: {intent}"
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1238** — bucket=**DEFECT**, pattern=`pass`, catch=`specific`, fn=`_resolve_operation`

  ```python
  except ValueError:
          pass
  ```

  _Reviewer note:_ silent pass — caller has no signal.

- **line 1242** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_resolve_operation`

  ```python
  except KeyError:
          return None
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

- **line 1309** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`execute_mutation`

  ```python
  except Exception as exc:
          return json.dumps(
              {
                  "status": "error",
                  "message": f"Error: invalid command — {exc}",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1320** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`execute_mutation`

  ```python
  except Exception as exc:
          logger.exception("execute_mutation_failed", operation=str(op))
          return json.dumps(
              {
                  "status": "error",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1389** — bucket=**GRACEFUL-DEGRADATION**, pattern=`log-only`, catch=`specific`, fn=`_install_shutdown_signal_handlers`

  ```python
  except (AttributeError, ValueError, OSError):
              logger.debug("mcp_server_signal_unsupported", signal=sig_name)
  ```

  _Reviewer note:_ logs and continues — likely best-effort; verify intent.

- **line 1416** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`main`

  ```python
  except Exception:
                  logger.exception("mcp_server_registry_close_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/migrate/graph_migrator.py` (9 sites — DEFECT=9)

- **line 147** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_read_all_current_nodes`

  ```python
  except Exception as exc:
              report.errors.append(("read_nodes", f"{type(exc).__name__}: {exc}"))
              logger.warning("graph_migration_read_failed", error=str(exc))
              return None
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 174** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_migrate_nodes`

  ```python
  except Exception as exc:
                  report.errors.append(
                      (f"node:{node_id}", f"get_node failed: {type(exc).__name__}: {exc}")
                  )
                  continue
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 198** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_migrate_nodes`

  ```python
  except Exception as exc:
                  report.errors.append(
                      (f"node:{node_id}", f"upsert failed: {type(exc).__name__}: {exc}")
                  )
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 217** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_migrate_aliases`

  ```python
  except Exception as exc:
                  report.errors.append(
                      (
                          f"aliases:{entity_id}",
                          f"get_aliases failed: {type(exc).__name__}: {exc}",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 235** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_migrate_aliases`

  ```python
  except Exception as exc:
                      report.errors.append(
                          (
                              f"alias:{source_system}:{raw_id}",
                              f"resolve failed: {type(exc).__name__}: {exc}",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 261** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_migrate_aliases`

  ```python
  except Exception as exc:
                      report.errors.append(
                          (
                              f"alias:{source_system}:{raw_id}",
                              f"upsert failed: {type(exc).__name__}: {exc}",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 287** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_migrate_edges`

  ```python
  except Exception as exc:
                  report.errors.append(
                      (
                          f"edges:{node_id}",
                          f"get_edges failed: {type(exc).__name__}: {exc}",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 327** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_migrate_edges`

  ```python
  except Exception as exc:
                      report.errors.append(
                          (
                              f"edge:{source_id}->{target_id}",
                              f"upsert failed: {type(exc).__name__}: {exc}",
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 342** — bucket=**DEFECT**, pattern=`return-empty`, catch=`Exception`, fn=`_destination_has_edge`

  ```python
  except Exception:
              return False
  ```

  _Reviewer note:_ broad catch (Exception); returns empty without logging — caller has no signal

#### `trellis/mutate/executor.py` (2 sites — DEFECT=1, GUARD=1)

- **line 175** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`execute`

  ```python
  except ValidationError as exc:
              log.warning("handler_rejected", errors=exc.errors)
              reason = exc.code if exc.code != "VALIDATION_ERROR" else "handler_validate"
              self._emit_rejection(command, reason=reason, message=str(exc))
              return CommandResult(
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 190** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`execute`

  ```python
  except Exception as exc:
              log.exception("handler_failed")
              self._emit(command, CommandStatus.FAILED, str(exc))
              return CommandResult(
                  command_id=command.command_id,
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/ops/recording.py` (1 sites — DEFECT=1)

- **line 90** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`record_outcome`

  ```python
  except Exception:
          logger.exception(
              "record_outcome.append_failed",
              component_id=component_id,
              event_id=event.event_id,
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/ops/registry.py` (1 sites — DEFECT=1)

- **line 121** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_resolve`

  ```python
  except Exception:
              logger.warning(
                  "parameter_registry.resolve_failed",
                  scope=key,
                  exc_info=True,
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/plugins/loader.py` (2 sites — DEFECT=2)

- **line 157** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`Exception`, fn=`discover`

  ```python
  except Exception:
          logger.exception("plugin_entry_points_lookup_failed", group=group)
          return []
  ```

  _Reviewer note:_ broad catch (Exception); logs warning then returns empty — classic silent-fallback shape

- **line 176** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`Exception`, fn=`load_class`

  ```python
  except Exception:
          logger.exception(
              "plugin_module_import_failed",
              group=spec.group,
              name=spec.name,
  ```

  _Reviewer note:_ broad catch (Exception); logs warning then returns empty — classic silent-fallback shape

#### `trellis/retrieve/budget_config.py` (1 sites — DEFECT=1)

- **line 80** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`from_dict`

  ```python
  except Exception:
              return cls()
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/retrieve/effectiveness.py` (1 sites — DEFECT=1)

- **line 919** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_emit_fitness_event`

  ```python
  except Exception:
          logger.exception(
              "advisory_fitness_event_emit_failed",
              event_type=event_type.value,
              advisory_id=advisory_id,
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/retrieve/pack_builder.py` (9 sites — DEFECT=9)

- **line 200** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`build`

  ```python
  except Exception:
                  logger.exception("strategy_failed", strategy=strategy.name)
                  continue
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 267** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`build`

  ```python
  except Exception:
                  logger.exception("reranker_failed", reranker=self._reranker.name)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 378** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`build_sectioned`

  ```python
  except Exception:
                  logger.exception("strategy_failed", strategy=strategy.name)
                  continue
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 412** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`build_sectioned`

  ```python
  except Exception:
                  logger.exception(
                      "reranker_failed_sectioned", reranker=self._reranker.name
                  )
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 565** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`Exception`, fn=`_attach_quality_report`

  ```python
  except Exception:
              logger.exception("pack_evaluator_failed", pack_id=pack.pack_id)
              return
  ```

  _Reviewer note:_ broad catch (Exception); logs warning then returns empty — classic silent-fallback shape

- **line 598** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_attach_quality_report`

  ```python
  except Exception:
                  logger.exception("pack_quality_event_emit_failed", pack_id=pack.pack_id)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 717** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_build_token_budget_payload`

  ```python
  except Exception:
                  logger.exception("token_budget_validator_failed")
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 744** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_recently_served_item_ids`

  ```python
  except Exception:
              logger.exception("session_dedup_event_query_failed")
              return set()
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 779** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`Exception`, fn=`_get_matching_advisories`

  ```python
  except Exception:
              logger.exception("advisory_retrieval_failed")
              return []
  ```

  _Reviewer note:_ broad catch (Exception); logs warning then returns empty — classic silent-fallback shape

#### `trellis/retrieve/semantic_seeds.py` (1 sites — DEFECT=1)

- **line 205** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`Exception`, fn=`extract`

  ```python
  except Exception:
              logger.exception(
                  "semantic_seed_extractor_query_failed",
                  top_k=self._top_k,
                  intent_preview=intent[:80],
  ```

  _Reviewer note:_ broad catch (Exception); logs warning then returns empty — classic silent-fallback shape

#### `trellis/retrieve/strategies.py` (3 sites — DEFECT=1, GUARD=2)

- **line 251** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_decay_importance_if_stale`

  ```python
  except (ValueError, TypeError):
              return importance
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 290** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_apply_recency_decay`

  ```python
  except (ValueError, TypeError):
          return base_score
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 726** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`build_strategies`

  ```python
  except Exception:
              logger.warning("semantic_search_init_failed", exc_info=True)
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/stores/advisory_store.py` (1 sites — DEFECT=1)

- **line 185** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_load`

  ```python
  except Exception:
              logger.exception("advisory_load_failed", path=str(self._path))
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/stores/arcadedb/base.py` (1 sites — GUARD=1)

- **line 139** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_http_request`

  ```python
  except urllib.error.HTTPError as exc:
          body_text = exc.read().decode() if exc.fp else ""
          return exc.code, body_text
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/stores/base/graph.py` (1 sites — GUARD=1)

- **line 39** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_resolve_max_subgraph_depth`

  ```python
  except ValueError:
          _logger.warning(
              "trellis_graph_max_subgraph_depth_invalid",
              raw=raw,
              fallback=6,
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/stores/bolt_opencypher/base.py` (1 sites — GUARD=1)

- **line 37** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`<module>`

  ```python
  except ImportError:
      HAS_NEO4J = False
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/stores/local/blob.py` (3 sites — GUARD=3)

- **line 109** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`sweep_expired`

  ```python
  except (OSError, json.JSONDecodeError):
                  errors += 1
                  logger.exception("blob_meta_read_failed", key=key)
                  continue
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 119** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`sweep_expired`

  ```python
  except (TypeError, ValueError):
                  errors += 1
                  logger.warning("blob_expires_at_parse_failed", key=key, value=raw)
                  continue
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 130** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`sweep_expired`

  ```python
  except OSError:
                      errors += 1
                      swept -= 1  # decrement — the delete failed
                      logger.exception("blob_delete_failed", key=key)
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/stores/neo4j/base.py` (1 sites — GUARD=1)

- **line 56** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`<module>`

  ```python
  except ImportError:
      _HAS_NEO4J_LOCAL = False
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/stores/pgvector/store.py` (3 sites — DEFECT=1, GUARD=2)

- **line 19** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`<module>`

  ```python
  except ImportError:
      HAS_PSYCOPG = False
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 26** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`<module>`

  ```python
  except ImportError:
      HAS_PGVECTOR = False
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 123** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_init_schema`

  ```python
  except (IndexError, ValueError):
              return
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

#### `trellis/stores/policy_store.py` (1 sites — DEFECT=1)

- **line 70** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_load`

  ```python
  except Exception:
              logger.exception("policy_load_failed", path=str(self._path))
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/stores/postgres/base.py` (1 sites — GUARD=1)

- **line 30** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_env_int`

  ```python
  except ValueError:
          return default
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/stores/registry.py` (19 sites — DEFECT=16, GRACEFUL-DEGRADATION=3)

- **line 270** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_get_merged_backends`

  ```python
  except Exception:
          logger.debug("plugin_loader_unavailable", store_type=store_type)
          _MERGED_BACKENDS_CACHE[store_type] = dict(builtins)
          return _MERGED_BACKENDS_CACHE[store_type]
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 315** — bucket=**GRACEFUL-DEGRADATION**, pattern=`return-empty`, catch=`Exception`, fn=`_try_llm_plugin`

  ```python
  except Exception:
          return None
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 330** — bucket=**GRACEFUL-DEGRADATION**, pattern=`log-return-empty`, catch=`Exception`, fn=`_try_llm_plugin`

  ```python
  except Exception:
              logger.exception(
                  failure_event,
                  provider=provider,
                  plugin=spec.value,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 391** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`_import_callable`

  ```python
  except Exception:
          logger.warning("embedding_fn_import_failed", path=dotted_path, exc_info=True)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 447** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`_build_openai_embedding_fn`

  ```python
  except ModuleNotFoundError:
          logger.warning("embedding_fn_openai_not_installed")
          return None
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 672** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`from_config_dir`

  ```python
  except Exception:
                  logger.warning("registry_config_load_failed", path=str(config_path))
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1097** — bucket=**DEFECT**, pattern=`return-empty`, catch=`Exception`, fn=`_resolve_substrate_class`

  ```python
  except Exception:
              return None
  ```

  _Reviewer note:_ broad catch (Exception); returns empty without logging — caller has no signal

- **line 1146** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`_load_fingerprint_meta`

  ```python
  except (OSError, ValueError):
              logger.warning("fingerprint_meta_read_failed", path=str(path))
              return {}
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 1163** — bucket=**GRACEFUL-DEGRADATION**, pattern=`log-only`, catch=`specific`, fn=`_write_fingerprint_meta`

  ```python
  except OSError:
              logger.warning("fingerprint_meta_write_failed", path=str(path))
  ```

  _Reviewer note:_ logs and continues — likely best-effort; verify intent.

- **line 1326** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`validate`

  ```python
  except Exception as exc:
                  errors.append((store_type, exc))
                  logger.warning(
                      "store_registry_validation_failed",
                      store_type=store_type,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1377** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_check_bolt_connectivity`

  ```python
  except Exception as exc:
                  failures.append((label, exc))
                  logger.warning(
                      "bolt_connectivity_check_failed",
                      uri=uri,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1522** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`build_llm_client`

  ```python
  except ModuleNotFoundError:
                  logger.debug("llm_client_sdk_not_installed", provider=provider)
                  return None
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 1533** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`build_llm_client`

  ```python
  except ModuleNotFoundError:
                  logger.debug("llm_client_sdk_not_installed", provider=provider)
                  return None
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 1543** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`build_llm_client`

  ```python
  except ModuleNotFoundError:
                  logger.debug("llm_client_sdk_not_installed", provider=provider)
                  return None
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 1554** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`build_llm_client`

  ```python
  except ModuleNotFoundError:
                  logger.debug("llm_client_sdk_not_installed", provider=provider)
                  return None
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 1630** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`build_embedder_client`

  ```python
  except ModuleNotFoundError:
                  logger.debug("embedder_client_sdk_not_installed", provider=provider)
                  return None
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 1641** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`build_embedder_client`

  ```python
  except ModuleNotFoundError:
                  logger.debug("embedder_client_sdk_not_installed", provider=provider)
                  return None
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 1701** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`close`

  ```python
  except Exception:
                  logger.warning("store_close_failed", store=type(store).__name__)
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 1707** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`close`

  ```python
  except Exception:
                  logger.warning("bolt_driver_close_failed", uri=key[0], user=key[1])
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis/stores/s3/blob.py` (4 sites — GUARD=4)

- **line 23** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`<module>`

  ```python
  except ImportError:
      HAS_BOTO3 = False
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 183** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`sweep_expired`

  ```python
  except ClientError:
                  errors += 1
                  logger.exception("blob_head_failed", key=key)
                  continue
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 193** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`sweep_expired`

  ```python
  except (TypeError, ValueError):
                  errors += 1
                  logger.warning("blob_expires_at_parse_failed", key=key, value=raw)
                  continue
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 207** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`sweep_expired`

  ```python
  except ClientError:
                      errors += 1
                      swept -= 1
                      logger.exception("blob_delete_failed", key=key)
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis/stores/sqlite/vector.py` (1 sites — GUARD=1)

- **line 15** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`<module>`

  ```python
  except ImportError:
      HAS_NUMPY = False
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

### `trellis_api/`

#### `trellis_api/observability.py` (4 sites — DEFECT=3, GRACEFUL-DEGRADATION=1)

- **line 56** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`_install_otel`

  ```python
  except ImportError:
          logger.debug(
              "otel_skipped_not_installed",
              extra="install with `pip install trellis-ai[observability]`",
          )
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 71** — bucket=**GRACEFUL-DEGRADATION**, pattern=`log-only`, catch=`specific`, fn=`_install_otel`

  ```python
  except ImportError:
          logger.debug("otel_psycopg_not_installed")
  ```

  _Reviewer note:_ logs and continues — likely best-effort; verify intent.

- **line 83** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`_install_prometheus`

  ```python
  except ImportError:
          logger.debug(
              "prometheus_skipped_not_installed",
              extra="install with `pip install trellis-ai[observability]`",
          )
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

- **line 122** — bucket=**DEFECT**, pattern=`log-only`, catch=`Exception`, fn=`install_observability`

  ```python
  except Exception as exc:
              logger.warning("otel_fastapi_instrument_failed", error=str(exc))
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis_api/routes/admin.py` (1 sites — DEFECT=1)

- **line 153** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`reset_vectors`

  ```python
  except Exception as exc:
          return {"status": "error", "message": str(exc)}
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis_api/routes/health.py` (2 sites — DEFECT=1, GUARD=1)

- **line 50** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_probe`

  ```python
  except Exception as exc:
          latency_ms = (time.monotonic_ns() - start_ns) / 1_000_000
          logger.warning(
              "readyz_probe_failed",
              backend=name,
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 84** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`readyz`

  ```python
  except RuntimeError:
          response.status_code = status.HTTP_503_SERVICE_UNAVAILABLE
          return {"status": "initializing"}
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis_api/routes/ingest.py` (2 sites — DEFECT=2)

- **line 113** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`upsert_vectors`

  ```python
  except Exception:
              errors += 1
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 295** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`ingest_bulk`

  ```python
  except Exception as exc:
              logger.warning(
                  "bulk_alias_failed",
                  entity_id=alias.entity_id,
                  source_system=alias.source_system,
  ```

  _Reviewer note:_ broad catch (Exception)

### `trellis_cli/`

#### `trellis_cli/admin.py` (10 sites — DEFECT=4, GUARD=6)

- **line 397** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`graph_health`

  ```python
  except Exception:  # noqa: S112
              continue
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 445** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`graph_health`

  ```python
  except Exception:  # noqa: S112
              continue
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 567** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_ensure_gitignore`

  ```python
  except FileNotFoundError:
          gitignore.write_text(xpg_line + "\n")
          return "gitignore_created"
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 693** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_memory_prompt_available`

  ```python
  except ImportError:
          return False
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

- **line 1151** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_check_healthz`

  ```python
  except httpx.HTTPError as exc:
          return _record_check("healthz", "fail", started, error=str(exc))
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 1167** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_check_readyz`

  ```python
  except httpx.HTTPError as exc:
          return _record_check("readyz", "fail", started, error=str(exc))
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 1186** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_check_auth_rejects_missing`

  ```python
  except httpx.HTTPError as exc:
          return _record_check("auth_rejects_missing", "fail", started, error=str(exc))
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 1202** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_check_auth_accepts_valid`

  ```python
  except httpx.HTTPError as exc:
          return _record_check("auth_accepts_valid", "fail", started, error=str(exc))
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 1218** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_check_metrics`

  ```python
  except httpx.HTTPError as exc:
          return _record_check("metrics", "fail", started, error=str(exc))
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 1251** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_safe_json`

  ```python
  except (ValueError, json.JSONDecodeError):
          return None
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

#### `trellis_cli/claude_integration.py` (1 sites — DEFECT=1)

- **line 32** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`read_claude_settings`

  ```python
  except FileNotFoundError:
          return {}
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

#### `trellis_cli/demo.py` (1 sites — DEFECT=1)

- **line 1350** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`load`

  ```python
  except Exception as exc:
              console.print(
                  f"  [yellow]![/yellow] Cold-start fixture failed: {exc}"
              )
  ```

  _Reviewer note:_ broad catch (Exception)

#### `trellis_cli/extract_refresh.py` (3 sites — DEFECT=3)

- **line 70** — bucket=**DEFECT**, pattern=`pass`, catch=`specific`, fn=`_resolve_extractor`

  ```python
  except ImportError:
          pass
  ```

  _Reviewer note:_ silent pass — caller has no signal.

- **line 119** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_snapshot_entities`

  ```python
  except Exception:
              snapshot[entity_id] = None
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 199** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`_emit_refresh_event`

  ```python
  except Exception:
          import structlog  # noqa: PLC0415
  
          structlog.get_logger(__name__).exception(
              "extract_refresh_emit_failed",
  ```

  _Reviewer note:_ broad catch (Exception)

### `trellis_sdk/`

#### `trellis_sdk/_http.py` (2 sites — DEFECT=2)

- **line 155** — bucket=**DEFECT**, pattern=`return-empty`, catch=`Exception`, fn=`_safe_json`

  ```python
  except Exception:
          return None
  ```

  _Reviewer note:_ broad catch (Exception); returns empty without logging — caller has no signal

- **line 183** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_parse_retry_after`

  ```python
  except (TypeError, ValueError):
          return None
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

#### `trellis_sdk/async_client.py` (1 sites — DEFECT=1)

- **line 144** — bucket=**DEFECT**, pattern=`return-empty`, catch=`specific`, fn=`_ensure_handshake`

  ```python
  except httpx.HTTPError:
                  return
  ```

  _Reviewer note:_ returns empty without logging — caller has no signal

#### `trellis_sdk/client.py` (1 sites — GUARD=1)

- **line 128** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_ensure_handshake`

  ```python
  except httpx.HTTPError:
              self._handshake_done = False
              return
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

### `trellis_workers/`

#### `trellis_workers/enrichment/service.py` (3 sites — DEFECT=1, GUARD=2)

- **line 164** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`enrich`

  ```python
  except Exception as e:
              logger.exception("enrichment_failed", title=title)
              return EnrichmentResult(success=False, error=str(e))
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 223** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_parse_response`

  ```python
  except json.JSONDecodeError as e:
              match = re.search(r"\{.*\}", text, re.DOTALL)
              if match:
                  try:
                      data = json.loads(match.group())
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

- **line 228** — bucket=**GUARD**, pattern=`other`, catch=`specific`, fn=`_parse_response`

  ```python
  except json.JSONDecodeError:
                      return EnrichmentResult(
                          success=False,
                          error=f"Invalid JSON: {e}",
                          raw_response=response,
  ```

  _Reviewer note:_ specific catch with non-trivial body — likely a guard; review on a case-by-case basis.

#### `trellis_workers/learning/miner.py` (2 sites — DEFECT=2)

- **line 179** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`Exception`, fn=`generate_precedent_candidates`

  ```python
  except Exception:
              logger.exception("precedent_generation_llm_error")
              return []
  ```

  _Reviewer note:_ broad catch (Exception); logs warning then returns empty — classic silent-fallback shape

- **line 211** — bucket=**DEFECT**, pattern=`log-return-empty`, catch=`specific`, fn=`_parse_candidates`

  ```python
  except (json.JSONDecodeError, ValueError):
              logger.warning("precedent_parse_error", response=response[:200])
              return []
  ```

  _Reviewer note:_ logs warning then returns empty — classic silent-fallback shape

#### `trellis_workers/maintenance/retention.py` (2 sites — DEFECT=2)

- **line 103** — bucket=**DEFECT**, pattern=`other`, catch=`Exception`, fn=`run`

  ```python
  except Exception as e:
                      report.errors.append(f"Error pruning {trace.trace_id}: {e}")
                      continue
  ```

  _Reviewer note:_ broad catch (Exception)

- **line 169** — bucket=**DEFECT**, pattern=`pass`, catch=`specific`, fn=`check`

  ```python
  except (ValueError, TypeError):
                      pass
  ```

  _Reviewer note:_ silent pass — caller has no signal.
