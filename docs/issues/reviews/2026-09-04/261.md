# Issue #261 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/261
- **Title:** Promote-to-standing advisory: graduate task-invariant items out of retrieval
- **Labels:** `blocked:signal`, `keystone`

## Verdict

**blocked-signal**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

AdvisoryGenerator exists. AdvisoryStore bootstrap if file missing (`src/trellis/mcp/server.py:152-157`). Additional blockers from handoff: #503 advisory cap; `pack_observations._join_one` docstring mentions `helpful_item_ids` but does not read them for per-item verdict; #502 fitness denominator undecided. Unblock when ≥30 attributed packs show recurring-item helpful rate above base **and** #502 owner decision recorded.

## Contradictions

Issue acceptance cites "useful in k consecutive sessions" without defining the integer; corpus cannot invent thresholds.

## Selected solution / decision

Defer implementation until the GitHub issue records concrete thresholds and standing destination. Fix `advisories.json` create-if-missing when built.

## Dependencies / collisions

- **Dependencies:** #255
- **Territories:** `src/trellis/retrieve/advisory_generator.py`

## Acceptance criteria (smallest testable)

After GitHub issue amendment: synthetic fixture with the recorded minimum distinct-session count emits promote-to-standing advisory; applying the documented lifecycle tag excludes the item from subsequent packs.

## Adversarial mutants / failure modes

Advisory injected into task packs; missing suppression tag (F14 duplication).

## Disposition acceptance

May move to implementation only after GitHub #261 records **(1)** concrete integer minimum distinct-session support, **(2)** concrete integer evaluation-window length in days, and **(3)** a named standing destination (for example `CLAUDE.md`, auto-memory index, or other path named in the issue). Until all three appear on the issue, verdict stays `blocked-signal` and status remains `not-started`.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | L |
| Wave | W5-learning |
| Status | not-started |
| PR | none |
