# Issue #463 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/463
- **Title:** Chunk rows decay off the import clock: CLASSIFY_METADATA_KEYS does not carry the source stamp
- **Labels:** `bug`

## Verdict

**blocked-signal**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

`resolve_recency_stamp` in strategies.py. Chunks inherit `CLASSIFY_METADATA_KEYS` only — no source `created_at`. **6/56 packs** served stamped parent + own chunk; keyword axis now scores them ~2.1× apart (consistency defect introduced by #462, not pre-existing). Owner comment (2026-09): chunk citation split rests on **small n** (7/38 vs 1/64 helpful/unhelpful) — **re-measure before acting**. P(cited helpful|served): parents 0.015, chunks 0.081, corpus 0.123 — propagating stamp would demote better-cited chunks.

## Contradictions

Parent/chunk split new on keyword axis post-#462; inherit-vs-import remains a real consistency question but not the worse-cited population.

## Selected solution / decision

Defer code until ≥30 packs serve stamped parent + own chunk **with per-item verdicts** (issue comment reopen criterion). Owner then chooses inherit source stamp vs keep import clock with citation regression test.

## Dependencies / collisions

- **Dependencies:** #417
- **Territories:** `src/trellis/classify/`, `src/trellis/retrieve/strategies.py`

## Acceptance criteria (smallest testable)

Signal threshold met; owner decision recorded; if inherit, citation regression on stamped parent+chunk packs.

## Adversarial mutants / failure modes

Fix chunk case alone without checking other derived-row paths; act on n=6 pack sample.

## Disposition acceptance

Reopen for implementation when attributed sample supports decision; until then verdict stays blocked-signal.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | M |
| Wave | W4-retrieval |
| Status | not-started |
| PR | none |
