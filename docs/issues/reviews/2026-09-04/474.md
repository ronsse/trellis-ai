# Issue #474 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/474
- **Title:** Confirm-to-save for global semantic memory (route save_memory through the executor)
- **Labels:** `enhancement`, `area:core-boundary`, `blocked:owner-decision`

## Verdict

**blocked-decision**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

save_memory direct `document_store.put` (see #360 call-site table). `require_approval` in policy gate is hard deny, not pending state. No owner-approved executable slice in GitHub comments (parent #478 only). Handoff recommendation: lean defer until incident measured — shape would be doc-plane `unconfirmed` stamp + gate + confirm, not theatre above ungoverned write.

## Contradictions

UX half blocked until #360 mechanism half ships; confirm above ungoverned write is theatre.

## Selected solution / decision

**No code slice authorized.** After #360 PR2: owner decides pending-state semantics (reuse extraction_status vocabulary vs new document lifecycle). MCP cannot block on human — pending + later commit shape only if owner approves.

## Dependencies / collisions

- **Dependencies:** #360 (governed write prerequisite, not sufficient for this issue)
- **Territories:** `src/trellis/mcp/server.py`, `src/trellis/mutate/`

## Acceptance criteria (smallest testable)

Owner records confirm semantics on GitHub; then governed write + opt-in confirm path; no direct put.

## Adversarial mutants / failure modes

Confirm step before executor routing; apply confirm to session-capture bulk path without owner call.

## Disposition acceptance

Owner-approved slice documented on issue before implementation PR.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | L |
| Wave | W4-mutate |
| Status | not-started |
| PR | none |
