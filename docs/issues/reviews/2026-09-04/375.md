# Issue #375 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/375
- **Title:** The graph axis has no query-relevant candidate path: all three seed producers are empty on the write path
- **Labels:** `enhancement`

## Verdict

**blocked-signal**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

**True premise (confirmed):** no production query-relevant graph seed path. `SemanticSeedExtractor` (`src/trellis/retrieve/semantic_seeds.py`) produced **0 seeds on 37/37** production intents in replay. `entity_aliases` empty; **82/685** servable nodes carry `document_ids`. `build_strategies(..., graph_seed_extractor=None)` at every in-repo call site (`src/trellis/retrieve/strategies.py:1530+`).

**Refuted proposed mechanisms (do not implement):** (1) auto-wire `SemanticSeedExtractor` — measured no-op; (2) alias/name scan without write-path mint — #369 substrate; (3) client-side full-graph name match — O(N) cap refusal.


## Contradictions

Issue title states a true defect; only the three named fix candidates are refuted by production replay.


## Selected solution / decision

Blocked pending a **new** write-path producer with production replay proof. Do not implement refuted candidates. Parent observability lives in #371.


## Dependencies / collisions

- **Dependencies:** #371 (parent observability)
- **Sequencing:** blocked until new producer designed

## Acceptance criteria (smallest testable)

Production replay: ≥1 intent yields ≥1 graph seed AND ≥1 pack `injected_items[]` differs from unseeded baseline; `tests/unit/retrieve/test_semantic_seeds.py::TestMemoryCorpusIsANoOp` fails when corpus satisfies extractor.


## Adversarial mutants / failure modes

Auto-wire SemanticSeedExtractor; raise GraphStore.query cap.

## Disposition acceptance

Production replay shows ≥1 graph seed on ≥1 real intent AND ≥1 pack `injected_items[]` differs from unseeded baseline; until then remain blocked.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | L |
| Wave | W5-retrieval |
| Status | not-started |
| PR | none |
