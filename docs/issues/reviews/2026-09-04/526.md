# Issue #526 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/526
- **Title:** 21 importorskip sites (~246 tests) execute in no workflow, and an importorskip reads as coverage while a deselected marker does not
- **Labels:** (none)

## Verdict

**valid-now**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

47 `pytest.importorskip(` call sites in tests/ (AST scan at review SHA; issue body says 48). `.github/workflows/tests.yml` installs `[dev]` only. `.github/workflows/live-infra.yml` path-selects five named paths. `pyproject.toml` addopts deselects backend markers.

## Contradictions

Issue claims 21 sites / 48 calls; current scan finds 47 calls. Grouped site count (27 in #525 framing) conflicts with 21-site rollup — canonical is #526 with corrected counts.

## Sub-slices

- W1a: Add tests.yml matrix leg with `.[all]` running full tests/ (primary fix).
- W1b: Fix publish.yml bogus `vectors` extra reference.
- W1c: Derived AST rule for importorskip vs workflow install/path coverage.
- W1d: Correct CLAUDE.md live-infra trigger wording.

## Selected solution / decision

One tests.yml matrix leg installing `.[all]` and running `tests/` — moves invisible skip to visible deselection. Do not patch per-extra in live-infra for pure-SDK tests.

## Dependencies / collisions

- **Dependencies:** (none)
- **Territories:** `tests/`, `.github/workflows/`

## Acceptance criteria (smallest testable)

Every importorskip site either executes in a workflow that installs its module, or appears on an explicit allow-list with reason; derived rule passes on synthetic evasion tree.

## Adversarial mutants / failure modes

Per-extra patch in live-infra only (category error); roster of file names without AST rule; vacuity guard dividing by scan output.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | L |
| Wave | W1-ci |
| Status | not-started |
| PR | none |
