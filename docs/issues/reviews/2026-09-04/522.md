# Issue #522 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/522
- **Title:** Rich still eats operator output at five renders the #492 handle rule does not claim, and line-wrapping is a third corruption nobody swept
- **Labels:** (none)

## Verdict

**valid-now**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

`tests/unit/test_rich_id_markup_rule.py` covers handle scope only. Five CLI renders outside scope (retrieve entity, classify domain-candidates, retrieve trace/pack Intent lines, curate promote-learning). Zero soft_wrap added in #492 sweep. `extract_refresh.py` typer Option branches skipped by mypy.

## Contradictions

Issue bundles three independent slices; each is independently shippable.

## Sub-slices

- 522a: Per-render soft_wrap/markup=False for five out-of-scope Rich renders.
- 522b: Loop-target blind spot in _hoisted_handle_names (migrate-graph legacy key).
- 522c: Two mypy-dark branches in extract_refresh.py:399 and :438.

## Selected solution / decision

Per-site markup=False or soft_wrap=True judgement. Extend AST rule for loop-target hoisting or hand-fix migrate-graph. Narrow extract_refresh Option types or add mypy reachability tests.

## Dependencies / collisions

- **Dependencies:** (none)
- **Territories:** `src/trellis_cli/`, `tests/unit/test_rich_id_markup_rule.py`

## Acceptance criteria (smallest testable)

FORCE_COLOR=1 and plain suites green at basetemp 39-46; five renders survive brackets/colons; mypy reaches else branches or types fixed.

## Adversarial mutants / failure modes

markup=False on whole call where literal also has brackets; concurrent pytest basetemp GC (20 spurious failures).

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | M |
| Wave | W1-ci |
| Status | not-started |
| PR | none |
