# Issue #342 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/342
- **Title:** Exercise the promote→review→serve chain in evals (decided: no production min_support=1 flag)
- **Labels:** `enhancement`

## Verdict

**valid-slice**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04
- **Plan:** [`plans/342.md`](./plans/342.md)

## Evidence (current main)

**Covered:** `tests/integration/loops/test_learning_promote_loop.py` — promote→review→persistence via REST packs, MCP `record_feedback`, CLI `analyze learning-candidates` / `curate promote-learning`; graph precedent node verified post-promotion.

**Uncovered (load-bearing):** promoted precedent appearing in an **ordinary subsequent `PackBuilder` pack** (`POST /packs` → `PACK_ASSEMBLED.injected_items[]`, not dedicated canonical doc id alone).

**Supplementary (not issue closure alone):** `get_lessons` / `list_precedents` read-path checks strengthen the loop but do not replace pack serving proof.

`eval/scenarios/parameter_registry_passthrough.py` (trellis-evals) calls real `analyze_learning_observations` with `min_support=1` — analyzer shape only.

## Contradictions

Recurrence wall refuted (45% items recur); promote→review→persistence is not refuted; ordinary pack serving remains open.

## Selected solution / decision

**Executable slice:** integration test asserts promoted `item_id` in `PACK_ASSEMBLED.injected_items[]` on subsequent ordinary pack assembly. Optional `get_lessons` / decoy-precedent checks supplementary. No production `min_support=1` flag.

## Dependencies / collisions

- **Dependencies:** (none)
- **Territories:** `tests/integration/loops/test_learning_promote_loop.py`

## Acceptance criteria (smallest testable)

Slice: pack-serving assertion on ordinary `POST /packs` after promotion OR owner documents close-on-persistence-only decision.

## Adversarial mutants / failure modes

Production min_support=1 flag; stub analyzer in evals; treating `get_lessons` alone as closure.

## Disposition acceptance

Promoted precedent `item_id` appears in subsequent ordinary `PackBuilder` pack `injected_items[]`. If owner declines pack assertion, close as satisfied on promote→review→persistence half only.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | M |
| Wave | W5-learning |
| Status | not-started |
| PR | none |
