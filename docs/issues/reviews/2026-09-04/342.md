# Issue #342 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/342
- **Title:** Exercise the promote→review→serve chain in evals (decided: no production min_support=1 flag)
- **Labels:** `enhancement`

## Verdict

**valid-slice**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

**Covered:** `tests/integration/loops/test_learning_promote_loop.py` — promote→review→persistence via REST packs, MCP `record_feedback`, CLI `analyze learning-candidates` / `curate promote-learning`; graph precedent node verified post-promotion.

**Uncovered:** promoted precedent appearing in an **ordinary subsequent `PackBuilder` pack** (`get_context` / `POST /packs` path, not dedicated canonical doc id `_HELPFUL_DOC_ID` alone).

`eval/scenarios/parameter_registry_passthrough.py` (trellis-evals) calls real `analyze_learning_observations` with `min_support=1` — analyzer shape only.


## Contradictions

Recurrence wall refuted (45% items recur); promote→review→persistence is not refuted; ordinary pack serving remains open.


## Selected solution / decision

**Executable slice:** add loop assertion that promoted precedent `item_id` appears in `PACK_ASSEMBLED.injected_items[]` on a subsequent ordinary pack assembly.

**Rescope decision criterion:** if owner declines pack-serving assertion, close issue as satisfied on persistence half; no production `min_support=1` flag.


## Dependencies / collisions

- **Dependencies:** (none)
- **Territories:** `tests/integration/loops/test_learning_promote_loop.py`

## Acceptance criteria (smallest testable)

Slice: one integration test proves promoted item served in subsequent `PackBuilder` pack OR owner documents close-on-persistence-only decision.


## Adversarial mutants / failure modes

Production min_support=1 flag; stub analyzer in evals.

## Disposition acceptance

**Rescope decision:** add assertion that promoted precedent `item_id` appears in `PACK_ASSEMBLED.injected_items[]` on a subsequent ordinary `PackBuilder` pack — if owner declines, close as satisfied on promote→review→persistence half only.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | M |
| Wave | W5-learning |
| Status | not-started |
| PR | none |
