# Issue #371 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/371
- **Title:** GraphSearch ignores its query in production: the graph axis is a recency feed, not a search
- **Labels:** `bug`, `enhancement`

## Verdict

**blocked-signal**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04

## Evidence (current main)

`GraphSearch.search` unseeded branch calls `GraphStore.query` = `ORDER BY created_at DESC` (`src/trellis/retrieve/strategies.py`, `stores/sqlite/graph.py:1234`, `stores/postgres/graph.py:1164`). `metadata["graph_selection"]` = `seeded` | `recency_window` forwarded to `PACK_ASSEMBLED.injected_items[]`. `build_strategies(..., graph_seed_extractor=None)` at all production call sites (`strategies.py:1530+`). Median **8.6%** servable-node coverage (37 packs, 30d to 2026-08-28).


## Contradictions

#375 mechanisms refuted; true premise (no query-relevant path) remains — see #375.


## Selected solution / decision

Observability shipped. Behaviour change blocked until #375 reopen criteria met (new write-path producer).


## Dependencies / collisions

- **Dependencies:** (none)
- **Territories:** `src/trellis/retrieve/strategies.py`

## Acceptance criteria (smallest testable)

When seeded, graph axis uses query-relevant seeds; coverage metric improves on replay harness.

## Adversarial mutants / failure modes

Wire SemanticSeedExtractor by default (0/37 measured no-op).

## Disposition acceptance

Write-path producer exists with replay proof (see #375 reopen criteria) before `build_strategies(graph_seed_extractor=...)` default changes.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | M |
| Wave | W5-retrieval |
| Status | not-started |
| PR | none |
