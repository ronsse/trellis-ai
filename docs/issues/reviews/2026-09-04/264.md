# Issue #264 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/264
- **Title:** North star: log every judged memory operation as a training example
- **Labels:** `ready`, `mechanical`

## Verdict

**valid-now**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04
- **Plan:** [`plans/264.md`](./plans/264.md)

## Evidence (current main)

`EventType.MEMORY_OP_JUDGED` (`src/trellis/stores/base/event_log.py:226`). Payload: `JudgedOpType` in `src/trellis/schemas/memory_op.py`. Emitters: shadow CLASSIFICATION (`classify/shadow.py:770+`, 999 prod); DISTILLATION (`session_capture/distill.py:346+`, 221 prod); RECONCILIATION (`mcp/reconcile.py:404+`, 0 prod). **Missing live paths:** `worker enrich` writes servable tags with no event; memory extraction (`run_memory_extraction`) emits nothing. **No CURATION emitter** — curation cycle is deterministic rules only.

## Contradictions

Label says mechanical but two emitters missing; shadow CLASSIFICATION ≠ live enrichment path.

## Selected solution / decision

PR-A: `emit_memory_op_judged` + enrichment CLASSIFICATION + EXTRACTION. PR-B: derived roster (reuse #514 `generate_call_sites` when present). No CURATION emitter.

## Dependencies / collisions

- **Dependencies:** (none in manifest); serialize PR-A with #360 PR2
- **Territories:** `src/trellis/stores/base/event_log.py`, `src/trellis/extract/`, `src/trellis/mcp/reconcile.py`

## Acceptance criteria (smallest testable)

EXTRACTION + live CLASSIFICATION emit; roster covers five generate sites; digest-not-content for restricted fixtures.

## Adversarial mutants / failure modes

Raw prompt in payload; CURATION emitter invented; parallel join path for labels.

## Disposition acceptance

EXTRACTION and live CLASSIFICATION emit `memory_op.judged`; derived roster green; one labeled triple materializable in trellis-evals.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | M |
| Wave | W2-learning |
| Status | not-started |
| PR | none |
