# Issue #360 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/360
- **Title:** The governed-pipeline hard rule does not hold for the document and vector planes
- **Labels:** `enhancement`

## Verdict

**valid-now**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04
- **Plan:** [`plans/360.md`](./plans/360.md)

## Evidence (current main)

create_curate_handlers: 13 ops, none document/vector create. ~18 document `.put`/`.delete` + 6 vector sites outside stores/handlers (see plan inventory). Retention handlers **do** write document metadata (`preserve_updated_at=True`) + vector mirror — exception to "no handler touches doc plane". **No governed document DELETE.** Worker `TraceSummaryIngestHandler` is only `EVIDENCE_INGEST` handler today (strict embed).

## Contradictions

Stage 2 policy still transparent empty gate — govern buys audit/idempotency not ACL yet.

## Selected solution / decision

PR1: governed-write AST rule + narrowed CLAUDE.md (staging roster only). PR2: core `EvidenceIngestHandler` (`embed=soft|strict`), route agent-facing writers. PR3+: metadata-only writers + batched ingest. **Issue stays open** until the non-backend direct-write roster is **empty** — staging roster entries are temporary and do **not** satisfy closure.

## Dependencies / collisions

- **Dependencies:** (none)
- **Territories:** `src/trellis/mutate/`, `src/trellis/mcp/server.py`, `src/trellis/core/vector_metadata.py`
- **Serialize PR2 with #264 PR-A** (save_memory overlap)

## Acceptance criteria (smallest testable)

PR1: roster rule fails on new direct write. PR2: agent-facing paths emit `evidence.ingest` + policy gate runs. **Full close:** non-backend direct-write roster **empty** (no remaining direct writes outside `stores/` and governed handlers).

## Adversarial mutants / failure modes

Worker-local handler only; one event per row on bulk ingest without batching; claiming PR2 closes issue.

## Disposition acceptance

All document/vector writes route through executor; contract test; batched emit for bulk ingest when roster clears corpus path. **Closure does not accept direct-write exceptions** — every staging roster entry must shrink to zero.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | L |
| Wave | W2-mutate |
| Status | not-started |
| PR | none |
