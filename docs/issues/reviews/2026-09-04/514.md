# Issue #514 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/514
- **Title:** Structured outputs vs. five hand-rolled JSON-forcing prompt sites (deferred from #500)
- **Labels:** `enhancement`

## Verdict

**valid-slice**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04
- **Plan:** [`plans/514.md`](./plans/514.md)

## Evidence (current main)

Five JSON-forcing `.generate(messages=...)` sites (hand-read): distill, reconcile, miner, enrichment/service, extract/llm. Four byte-similar fence strippers; distill malformed indistinguishable from empty session.

## Contradictions

Structured outputs need protocol change; fence consolidation does not.

## Sub-slices

- **514a valid-now:** shared `parse_json_response` + per-caller fallback preserved + distill `sessions_judge_malformed` counter.
- **514b out of slice:** `response_schema` on LLMClient until malformed rate measured.

## Selected solution / decision

514a: one seam, explicit salvage per caller, derived AST rule. No provider capability flag in this slice. Structured outputs explicitly deferred.

## Dependencies / collisions

- **Dependencies:** (none); #264 roster reuses `generate_call_sites` when landed (adjacency)
- **Territories:** `src/trellis_workers/`, `src/trellis/mcp/reconcile.py`, `src/trellis/extract/llm.py`

## Acceptance criteria (smallest testable)

514a: single helper used by all five sites; metric emitted or logged when fallback fires. 514b: explicit defer.

## Adversarial mutants / failure modes

Per-site fence copy survives consolidation; silent [] from distill indistinguishable from empty session.

## Disposition acceptance

Shared fence helper used by all five JSON sites; fallback fire counts observable; structured-output half remains explicitly deferred.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | L |
| Wave | W3-llm |
| Status | not-started |
| PR | none |
