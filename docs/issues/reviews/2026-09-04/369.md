# Issue #369 — review brief

## Source

- **URL:** https://github.com/ronsse/trellis-ai/issues/369
- **Title:** Name-alias resolution stops learning past DEFAULT_NAME_SCAN_LIMIT, and starts answering wrongly
- **Labels:** `enhancement`

## Verdict

**valid-now**

- **Reviewed SHA:** `f9ff32c933020e267f5560493f4739538d2b95f6`
- **Reviewed at:** 2026-09-04
- **Plan:** [`plans/369.md`](./plans/369.md)

## Evidence (current main)

build_name_alias_resolver fallback scan GraphStore.query limit=DEFAULT_NAME_SCAN_LIMIT (2000). Past cap: miss → LLM residue → twin ULID mint (worse than issue states). ~964 nodes, ~34-60 days to cap. Zero entity_aliases in production (memory extraction off).

## Contradictions

Production zero aliases looks like health but is dark feature.

## Selected solution / decision

`bind_name_alias` on EntityCreate/Update + bounded `trellis admin backfill-name-aliases` (truncated backfill binds nothing). First-wins at write time.

## Dependencies / collisions

- **Dependencies:** none (shipped `normalize_entity_name` in `well_known.py` is the prerequisite — trim, collapse whitespace, casefold+NFC; punctuation preserved)
- **Note:** #375 `NameAliasSeedExtractor` needs this merged + production backfill — not a universal manifest dependency on #369
- **Territories:** `src/trellis/mutate/`, `src/trellis_cli/`

## Acceptance criteria (smallest testable)

Resolver hits alias index for pre-cap entities; mintable true on write path; backfill idempotent.

## Adversarial mutants / failure modes

Raise cap only; properties={name:...} retry without mintable restoration.

## Disposition acceptance

Alias mint on write + backfill; resolver hits index for pre-cap entities in contract test.

## Size / wave / execution

| Field | Value |
|-------|-------|
| Size | M |
| Wave | W2-mutate |
| Status | not-started |
| PR | none |
