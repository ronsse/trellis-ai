Cluster C (advisory/feedback loop)
#503 DEFER — category-blind cap real, but ENTITY advice is actionable when entity ABSENT, ANTI_PATTERN when PRESENT; interleaving without a presence semantic serves rows about items not in pack → coupled to #502. Wrong claims: ANTI_PATTERN also carries entity_id; `injected_advisory_ids` has NO reader in src/; attribution.py:100-113 docstring false. Fold `advisories_matched_by_category` counter + 3 docstring fixes into #502's PR.
#502 OWNER — Q: judge advisories against a comparison arm (ε-slot 1-in-5 + lift_vs_baseline w/ generator support gate) or keep absolute blend (loop can only suppress all-or-nothing → #502 moot)? Recommend comparison arm + lift. Deeper defect: blend `0.7*old+0.3*success_rate`, suppress <0.1, keys on ABSOLUTE rate → fixed point = deployment base rate; any base rate >~0.21 boosts every advisory regardless of lift. Then exploration slot = CODE S/M.
#364 CLOSE — resolved by PR #389 (2ff6143): coverage reported beside the ratio (judged_token_coverage / unjudged_token_fraction, interval width pinned). Draft close comment in planner report (transcript).
#342 CODE S — plans/342.md. Two of three checkboxes already pass in CI (test_learning_promote_loop.py + live-infra runs tests/integration/loops/ on PR). Only "served" assertion missing: assert precedent_event_status=="success", seed decoy precedent (population 2), assert via MCP get_lessons + list_precedents. Needs live-infra run.
#261 DEFER — keep blocked. New blockers: #503 cap, pack_observations._join_one has no per-item verdict (docstring :53 mentions helpful_item_ids it never reads), #502. Unblock: ≥30 attributed packs w/ recurring-item helpful rate above base AND #502 decided.
