# #256 — Bolt backends (neo4j / arcadedb) out of core: seam-proving first step only

**Defect statement.** `StoreRegistry._instantiate` (src/trellis/stores/registry.py:887–986) hard-codes backend-name-keyed construction logic for `neo4j` and `arcadedb` (driver sharing, env fallbacks, ensure-database, provenance migration) and imports `trellis.stores.neo4j.base` / `trellis.stores.arcadedb.base` / `bolt_opencypher.base` to do it — so a backend cannot be moved out of the `trellis` package even though the plugin loader already resolves classes by entry point. Removing the packages / extras from the published `trellis-ai` wheel is OWNER (publish surface); making it *possible* is CODE.

## Verified premise (main-ref)

- **Stale claim:** "the registry must learn to resolve the plugin via entry point" — already shipped. `src/trellis/plugins/loader.py` (`GROUP_STORES="trellis.stores"`, per-type `trellis.stores.<type>`, `discover`, `merge_with_builtins`, builtins win unless `OVERRIDE_ENV`), consumed by `_get_merged_backends` (registry.py:280, with `_MERGED_BACKENDS_CACHE`/`_reset_backend_cache`); tests in tests/unit/plugins/test_loader.py; contract in docs/design/adr-plugin-contract.md. Verified.
- **Verified true:** the real coupling is in `_instantiate`: `if backend == "neo4j" and "driver" not in params: params = self._inject_neo4j_driver(params)` (imports `trellis.stores.neo4j.base`, :988); `if backend == "arcadedb" and store_type == "graph": _inject_arcadedb_driver` (:1043) / `elif … vector: _resolve_arcadedb_vector_params`; `_parse_bolt_driver_config` (:653, imports `bolt_opencypher.base`); `_bolt_drivers` cache (:718) shared across graph+vector so one Bolt driver serves both; `_arcadedb_provenance_migrated` (:726) + `_ensure_arcadedb_provenance_migrated_on_cached_driver` (:1237); `close()` closes `_bolt_drivers`. `_EXTRA_FOR_BACKEND` (:193) maps `neo4j`/`arcadedb` to their extras for `BackendNotInstalledError`. Other core references are text only: `errors.py:57`, `retrieve/strategies.py` comments, `trellis_api/routes/health.py:82`, `trellis_cli/admin.py:1565` help.
- Bolt code = 3332 LOC across `stores/{neo4j,arcadedb,bolt_opencypher}`; tests `tests/unit/stores/test_{arcadedb_*,neo4j_*,bolt_opencypher_provenance}.py`, `contracts/test_{arcadedb,neo4j}_graph_contract.py`; registry-coupled tests: `test_arcadedb_credential_split.py`, `test_arcadedb_registry_provenance.py`.
- pyproject: extras `neo4j` (:100), `arcadedb` (:103 — also depends on the neo4j driver), wheel packages :145; `.github/workflows/publish.yml` publishes `trellis-ai` via OIDC → the extras are a **published** surface. `live-infra.yml` installs `.[dev,cloud,neo4j]` with `neo4j:2025.12`; the ArcadeDB contract runs nowhere (#351).
- Measurement: none needed; the change is structural. Evidence = test suite + a synthetic plugin package in `tests/`.

## The change (one PR, M)

1. **Backend-side preparation hook.** In `src/trellis/stores/base/` (or `plugins/contract.py` if that exists) add a Protocol:
   ```python
   class RegistryPreparable(Protocol):
       @classmethod
       def prepare_registry_params(cls, ctx: RegistryContext, store_type: str, params: dict[str, Any]) -> dict[str, Any]: ...
   ```
   `RegistryContext` is a small frozen object the registry passes: `env` (a read-only mapping), `shared: dict[str, Any]` (the driver cache — today `_bolt_drivers`), `register_closer(callable)`, `emit_warning(msg)`. Nothing backend-specific.
2. **Move the branches onto the classes.** `Neo4jGraphStore.prepare_registry_params` absorbs `_inject_neo4j_driver` + `_parse_bolt_driver_config`; `ArcadeDBGraphStore.prepare_registry_params` absorbs `_inject_arcadedb_driver` + provenance-migration bookkeeping (state moves from `registry._arcadedb_provenance_migrated` into `ctx.shared["arcadedb:provenance_migrated"]`); `ArcadeDBVectorStore.prepare_registry_params` absorbs `_resolve_arcadedb_vector_params`; `Neo4jVectorStore` shares the driver via `ctx.shared`. `_instantiate` becomes: builtin sqlite/local/postgres/pgvector/s3 branches unchanged, then `if hasattr(cls, "prepare_registry_params"): params = cls.prepare_registry_params(ctx, store_type, params)`, then `cls(**params)`. The registry no longer imports any bolt module.
3. **`_EXTRA_FOR_BACKEND`** stays (it is an error-message hint and cheap); note in a comment that it is the last name-keyed reference and is removed with the packages.
4. **Prove the seam with a synthetic plugin in tests**, not by moving the packages: `tests/unit/plugins/test_registry_prepare_hook.py` builds a fake backend class exposing `prepare_registry_params` that (a) reads `ctx.shared`, (b) registers a closer, (c) mutates params; registers it via a monkeypatched entry point (the loader already has fixtures for this in test_loader.py) and asserts `StoreRegistry` instantiates it with the prepared params, shares state between two store types from the same plugin, and calls the closer on `close()`.
5. **Optional in-repo package skeleton** `packages/trellis-stores-bolt/pyproject.toml` declaring `[project.entry-points."trellis.stores.graph"] neo4j = "trellis.stores.neo4j:Neo4jGraphStore"` etc. **against the still-in-core modules** — proves the entry-point wiring resolves the same classes, changes nothing about the trellis-ai wheel, and is deletable. Deliberately *not* moving files.

## Tests

- Fail before / pass after: `test_registry_prepare_hook.py` (hook does not exist before). Existing `test_arcadedb_credential_split.py`, `test_arcadedb_registry_provenance.py`, `test_registry_close.py`, `test_registry_backend_not_installed.py` must pass unchanged — they are the regression net for the moved logic (if a test reaches into `registry._inject_neo4j_driver` directly, update it to call the class hook; say so in the PR).
- Mutants: hook not called in `_instantiate` (synthetic-plugin test dies); closer not invoked on `close()` (dies); `ctx.shared` a fresh dict per call (driver-sharing test between graph and vector dies — fixture must have **two** store types from the plugin); provenance-migration flag not carried (existing `test_arcadedb_registry_provenance.py` dies).
- Derived rule: add to the shared AST rule table (`tests/unit/test_*_rule.py` pattern, #497) an assertion that `src/trellis/stores/registry.py` contains no `Import`/`ImportFrom` of `trellis.stores.neo4j`, `trellis.stores.arcadedb`, `trellis.stores.bolt_opencypher` and no string literal comparison `backend == "neo4j"` / `"arcadedb"` outside `_EXTRA_FOR_BACKEND`. Hand-read floor: today 3 imports + 4 name comparisons — the test must first be shown to fail on f9ff32c (record that count), then pass.

## Non-goals

- Not moving `stores/{neo4j,arcadedb,bolt_opencypher}` out of `src/trellis/`, not deleting the `neo4j`/`arcadedb` extras, not adding a second distribution to publish.yml — each changes the published wheel → OWNER.
- Not #351 (ArcadeDB CI container). It is a CI edit, in scope, but a separate PR; note that the extraction *cannot* be finished until the ArcadeDB contract runs somewhere, so #351 is a prerequisite of the removal step.
- Not touching Neo4j vector shape #2 semantics.

## Risks the gate will probe

- `ctx.shared` becomes an untyped bag; a reviewer will ask why not a typed field. Answer: the registry must not know the driver type — that is the whole point — but key the bag by backend module name and document it.
- Provenance migration runs once per cached driver (registry.py:1237); moving that bookkeeping onto the class must keep the "once per driver, not once per store" property — the existing test pins it; make sure the mutant "run per store" dies.
- Does the hook also need `validate()`/fingerprint access (registry.py `validate`, `test_registry_fingerprint.py`)? Check whether fingerprinting reads driver config; if so, the hook must not change what is fingerprinted.

## Size / ordering

M. Independent of #360. Removal + publish step is OWNER; #351 precedes removal.

## OWNER half (recorded here for the report)

Question: after the seam lands, do you want the bolt backends published as a **separate distribution** (`trellis-stores-bolt`, new publish surface, new PyPI name) or kept in-tree behind the existing extras indefinitely? Recommended: keep in-tree behind extras until a second external backend plugin exists — the seam removes the coupling, and a second wheel adds a publish/versioning surface with no consumer yet.
